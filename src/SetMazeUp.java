import java.awt.GridBagConstraints;
import java.awt.GridBagLayout;
import javax.swing.ImageIcon;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JPanel;

class SetMazeUp
{
   /*
   ImageIcon [] tempIcons = new ImageIcon[16*16];
   JLabel [] tempLabels = new JLabel[16*16];
   JPanel[] panel = new JPanel[16*16];
   CreateSpriteObjects spriteObject = new CreateSpriteObjects();
   ImageIcon[] iconArray = new ImageIcon[spriteObject.borderArray.length];
   ImageIcon blackImage = new ImageIcon(spriteObject.blackImage);
   ImageIcon circleImage = new ImageIcon(spriteObject.circleImage);
   ImageIcon pacMan = new ImageIcon(spriteObject.pacManArray[0].getImage());
   ImageIcon ghosts = new ImageIcon(spriteObject.ghostArray[1].getImage());
   
   SetMazeUp()
   {
      gbc.gridheight = 50;
      gbc.gridwidth = 50;
      gbc.gridx = 0;
      gbc.gridy= 0;
      
      maze.setSize(800, 825);
      maze.setLayout(layout);
      
      for (int i = 0; i < spriteObject.borderArray.length; i++)
         iconArray[i] = new ImageIcon(spriteObject.borderArray[i].getImage());
      
      maze.setVisible(true);
      maze.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
   }
   
      */

}

